# Loaded by Vite when building/serving with `--mode demo`.
# Enables the fully-static, no-backend demo runtime (fetch + WebSocket shims).
# See DEMO.md. Has zero effect on the normal `npm run build`.
VITE_DEMO=1
