## 2024-05-23 - Accessibility in Login Forms
**Learning:** React components often use `div` wrappers without `htmlFor`/`id` association, breaking accessibility.
**Action:** Always verify `htmlFor` matches `id` in new forms, and prefer semantic `form` structure.
